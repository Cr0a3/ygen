# RUN: 
cargo run -p ylc -- -in=%s -asm-clr --triple=x86_64-unknown-linux | filecheck %s

# IN:
define ptr @test() {
  entry:
; CHECK: lea ecx, [ rsp - 8 ]
    %1 = alloca i32
; CHECK: mov eax, ecx
    ret ptr %1
}